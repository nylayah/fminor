// holds easy mode

// only on chord or note displays with associated chord/note label


const pianoRange = {
    //A0 - > C8
}

const bassRange = {
    // E2 -> C4
}

const guitarRange = {
    // E2 -> E4
}

const violinRange = {
    // G3 -> A7
}